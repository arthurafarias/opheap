$kind: http-request
url: "{{baseUrl}}/v1/credentials/{{primaryPrincipal}}"
method: DELETE
headers:
  X-Admin-Token: "{{adminToken}}"
scripts:
  - type: afterResponse
    code: pm.test('status code is 200', function () { pm.response.to.have.status(200); });
    language: text/javascript
order: 2000
