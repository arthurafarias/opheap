#include <opheap/opheap.hpp>

#include <cstdio>

// Placeholder entry point. opheap-cli will become a command-line tool for
// inspecting and administering opheap stores (heap.wal, snapshots).
int main() {
    std::puts("opheap-cli: not yet implemented");
    return 0;
}
